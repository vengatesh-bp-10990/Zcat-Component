"use strict";
(self["webpackChunkzcat_app"] = self["webpackChunkzcat_app"] || []).push([["vendors-node_modules_zoho_lyte-ui-component_components_javascript_lyte-radiobutton_js"],{

/***/ 43318909:
/*!****************************************************************************************!*\
  !*** ./node_modules/@zoho/lyte-ui-component/components/javascript/lyte-radiobutton.js ***!
  \****************************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "LyteRadiobuttonComponent": () => (/* binding */ LyteRadiobuttonComponent)
/* harmony export */ });
/* harmony import */ var _slyte_core_index_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../../../@slyte/core/index.js */ 60469700);
/* harmony import */ var _component_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../component.js */ 98708570);




/**
 * Renders a radiobutton
 * @component lyte-radiobutton
 * @version 1.0.0
 * @utility focus,blur,click
 * @methods onBeforeChecked,onBeforeUnchecked,onChecked,onUnchecked,onChanged
 */

if (!window._lyteUiUtils.radio) {
	window._lyteUiUtils.radio = {};
}

class LyteRadiobuttonComponent extends _component_js__WEBPACK_IMPORTED_MODULE_0__.Component {
    constructor() {
        super();
        this._lyteUtilFunctions = [ 'focus', 'click', 'blur' ];
    }

    data(arg1) {
		return Object.assign(super.data({

			/**
			 * @componentProperty {default|primary|secondary|switch|slider} ltPropType
			 * @default default
			 */

			'ltPropType': (0,_slyte_core_index_js__WEBPACK_IMPORTED_MODULE_1__.prop)('string', {
				'default': window._lyteUiUtils.resolveDefaultValue('lyte-radiobutton', 'type', 'default')
			}),

			/**
			 * @componentProperty {string} ltPropName
			 */

			'ltPropName': (0,_slyte_core_index_js__WEBPACK_IMPORTED_MODULE_1__.prop)('string', {
				'default': undefined
			}),

			/**
			 * @componentProperty {boolean} ltPropDisabled
			 * @default false
			 *
			 */

			'ltPropDisabled': (0,_slyte_core_index_js__WEBPACK_IMPORTED_MODULE_1__.prop)('boolean', {
				'default': false
			}),

			/**
			 * @componentProperty {boolean} ltPropChecked
			 * @default false
			 *
			 */

			'ltPropChecked': (0,_slyte_core_index_js__WEBPACK_IMPORTED_MODULE_1__.prop)('boolean', {
				'default': false
			}),

			/**
			 * @componentProperty {string} ltPropLabel
			 */


			'ltPropLabel': (0,_slyte_core_index_js__WEBPACK_IMPORTED_MODULE_1__.prop)('string', {
				'default': undefined
			}),

			/**
			 * @componentProperty {string} ltPropValue
			 */
			'ltPropValue': (0,_slyte_core_index_js__WEBPACK_IMPORTED_MODULE_1__.prop)('string', {
				'default': undefined
			}),

			/**
			 * @componentProperty {boolean} ltPropFireOnInit
			 * @default false
			 *
			 */

			'ltPropFireOnInit': (0,_slyte_core_index_js__WEBPACK_IMPORTED_MODULE_1__.prop)('boolean', {
				'default': window._lyteUiUtils.resolveDefaultValue('lyte-radiobutton', 'fireOnInit', false)
			}),

			/**
			 * @componentProperty {string} ltPropLabelClass
			 */
			'ltPropLabelClass': (0,_slyte_core_index_js__WEBPACK_IMPORTED_MODULE_1__.prop)('string', {
				'default': window._lyteUiUtils.resolveDefaultValue('lyte-radiobutton', 'labelClass', '')
			}),

			/**
			 * @componentProperty {string} ltPropClass
			 */

			'ltPropClass': (0,_slyte_core_index_js__WEBPACK_IMPORTED_MODULE_1__.prop)('string', {
				'default': window._lyteUiUtils.resolveDefaultValue('lyte-radiobutton', 'class', '')
			}),

			/**
			 * @componentProperty {boolean} ltPropYield
			 * @version 2.2.8
			 * @default false
			 *
			 */

			'ltPropYield': (0,_slyte_core_index_js__WEBPACK_IMPORTED_MODULE_1__.prop)('boolean', {
				'default': false
			}),
			'ltPropAriaRadio': (0,_slyte_core_index_js__WEBPACK_IMPORTED_MODULE_1__.prop)('object', {
				'default': window._lyteUiUtils.resolveDefaultValue('lyte-radiobutton', 'ariaRadio', {})
			}),
			'ltPropFocus': (0,_slyte_core_index_js__WEBPACK_IMPORTED_MODULE_1__.prop)('boolean', {
				'default': false
			}),

			'ltPropPreventFocus': (0,_slyte_core_index_js__WEBPACK_IMPORTED_MODULE_1__.prop)('boolean', { 'default': false }),

			'lyteUnbound': (0,_slyte_core_index_js__WEBPACK_IMPORTED_MODULE_1__.prop)('boolean', {
				'default': false
			}),

			'ltPropReadOnly': (0,_slyte_core_index_js__WEBPACK_IMPORTED_MODULE_1__.prop)('boolean', {
				'default': false
			}),
			'ltPropDataTabindex': (0,_slyte_core_index_js__WEBPACK_IMPORTED_MODULE_1__.prop)('string', { 'default': "" }),

			'ltPropId': (0,_slyte_core_index_js__WEBPACK_IMPORTED_MODULE_1__.prop)( 'string', { 'default': '' } ),

			ltPropKeyboardNavigation : (0,_slyte_core_index_js__WEBPACK_IMPORTED_MODULE_1__.prop)("boolean",{"default":true}),

			'ltPropTabindex': (0,_slyte_core_index_js__WEBPACK_IMPORTED_MODULE_1__.prop)( 'string', { 'default': undefined } )
		}), arg1);
	}

    getFromCache(keyName) {
		var cache = window._lyteUiUtils.radio,
			name = this.getData('ltPropName') || '',
			radioGroup = cache[name] || {};

		return radioGroup[keyName];
	}

    setInCache(keyName, value) {
		var cache = window._lyteUiUtils.radio,
			name = this.getData('ltPropName') || '',
			radioGroup = cache[name];

		if (radioGroup) {
			radioGroup[keyName] = value;
		}
	}

    didDestroy() {
		this.destroyCache();
		delete this.$node.focus;
		delete this.$node.blur;
		delete this.$node.click;
	}

    destroyCache() {
		var name = this.getData('ltPropName'),
			value = this.getData('ltPropValue');

		if (this.getFromCache('prev') === value) {
			this.setInCache('prev', undefined);
		}

		if (this.getFromCache('node') === this.$node.querySelector('input')) {
			this.setInCache('node', undefined);
		}

		var index = (this.getFromCache('peers') || []).indexOf(this.$node);

		if (!!~index) {
			this.getFromCache('peers').splice(index, 1);
		}
	}

    changeStyleFunction() {

		var type = this.getData('ltPropType'),
			checked = this.getData('ltPropChecked'),
			cls = this.getData('ltPropClass'),
			labelCls = this.getData('ltPropLabelClass');

		if (
			type.indexOf('slider') === -1
			&& type.indexOf('switch') === -1
			&& type.indexOf('default') === -1
			&& type.indexOf('primary') === -1
			&& type.indexOf('secondary') === -1
			&& type.indexOf('checkbox') === -1
		) {
			this.setData('ltPropType', 'default');
		}

		if (type.indexOf('default') !== -1) {
			this.setData('radioclass', 'lyteRadioBtn lyteDefault')
			this.setData('ltPropClass', cls ? cls : 'lyteRadioLayer')
			this.setData('ltPropLabelClass', labelCls ? labelCls : 'lyteRadioLabel')
		}
		else if (type.indexOf('primary') !== -1) {
			this.setData('radioclass', 'lyteRadioBtn lytePrimary')
			this.setData('ltPropClass', cls ? cls : 'lyteRadioLayer')
			this.setData('ltPropLabelClass', labelCls ? labelCls : 'lyteRadioLabel')
		}
		else if (type.indexOf('secondary') !== -1) {
			this.setData('radioclass', 'lyteRadioBtn lyteSecondary')
			this.setData('ltPropClass', cls ? cls : 'lyteRadioLayer')
			this.setData('ltPropLabelClass', labelCls ? labelCls : 'lyteRadioLabel')
		}
		else if (type.indexOf('switch') !== -1) {
			this.setData('ltPropClass', cls ? cls : 'lyteRadioSwitch')
			this.setData('ltPropLabelClass', labelCls ? labelCls : 'lyteRadioLabel')
		}
		else if (type.indexOf('slider') !== -1) {
			this.setData('ltPropClass', cls ? cls : 'lyteRadioSlider')
			this.setData('ltPropLabelClass', labelCls ? labelCls : 'lyteRadioSliderText')
		}
		else if (type.indexOf('checkbox') !== -1) {
			this.setData('ltPropClass', cls ? cls : 'lyteRadioCheckbox')
			this.setData('ltPropLabelClass', labelCls ? labelCls : 'lyteRadioCheckboxLabel')
		}
	}

    reduceOpacity() {
		if (this.getData('ltPropDisabled')) {
			this.$node.classList.add('lyteRadioDisabled');
		}
		else {
			this.$node.classList.remove('lyteRadioDisabled');
		}
	}

    init() {

		this.changeStyleFunction();
		this.setRandomId();
	}

    initCache() {
		var name = this.getData('ltPropName');

		if (!name) {
			return;
		}

		if (!window._lyteUiUtils.radio[name]) {
			window._lyteUiUtils.radio[name] = {};
		}

		if (!window._lyteUiUtils.radio[name].peers) {
			window._lyteUiUtils.radio[name].peers = [];
		}

		window._lyteUiUtils.radio[name].peers.push(this.$node);
	}

    setRandomId() {
		this.setData('randomId', 'lyte-radiobutton-label-' + window._lyteUiUtils.rbuttonId++);
	}

    didConnect() {
		this.warnUsers();
		var that = this, newAria = this.getData('ltPropAriaRadio');

		var dataIndex = this.getData('ltPropDataTabindex')
		this.$node.setAttribute('data-tabindex', dataIndex)

		this.initCache();
		this.reduceOpacity();

		this.$node.click = function () {
			var ev = new Event('click', {
				bubbles: true,
				cancelable: true
			}),
				node = that.$node,
				checked = node.ltProp('checked'),
				disabled = node.ltProp('disabled');

			if (disabled) {
				return;
			}

			node.component.clickFn = true;

			if (!checked) {
				if (node._fR) {
					// Just calling resetValues here to make things less complicated
					node.component.resetValues();
					node.component.checkFR(node);
				}
				else {
					node.ltProp('checked', true);
				}
			}

			node.component.clickFn = false;

			node.dispatchEvent(ev);
		}

		this.$node.focus = function () {
			var node = that.$node,
				input = node.querySelector('input'),
				disabled = node.ltProp('disabled');

			if (disabled) {
				return;
			}

			input.focus();
		}

		this.$node.blur = function () {
			var node = that.$node,
				input = node.querySelector('input'),
				disabled = node.ltProp('disabled');

			if (disabled) {
				return;
			}

			input.blur();
		}

		this.callMethodsOnInit(true);
		this.addAriaValues({}, newAria);
	}

    warnUsers() {
		var name = this.getData('ltPropName') || '',
			value = this.getData('ltPropValue') || '';

		if (!name) {
			console.warn('lt-prop-name attribute not present in radiobutton. It is required', this.$node);
		}

		if (!value) {
			console.warn('lt-prop-value attribute not present in radiobutton. It is required', this.$node);
		}
	}

    addAriaValues(oldAria, newAria) {
		var radio = this.getRadioWidget();

		window._lyteUiUtils.setAttribute(radio, newAria, oldAria);
	}

    getRadioWidget() {
		return this.$node.querySelector('input');
	}

    check(inp, comp) {
		if (this.getMethods('onChecked')) {
			this.executeMethod('onChecked', inp, comp, this.eventType());
		}
	}

    uncheck(inp, comp) {
		if (this.getMethods('onUnchecked')) {
			this.executeMethod('onUnchecked', inp, comp, this.eventType());
		}
	}

    beforeCheck(inp, comp) {
		if (this.getMethods('onBeforeChecked')) {
			return this.executeMethod('onBeforeChecked', inp, comp, this.eventType());
		}
	}

    beforeUncheck(inp, comp) {
		if (this.getMethods('onBeforeUnchecked')) {
			return this.executeMethod('onBeforeUnchecked', inp, comp, this.eventType());
		}
	}

    onchanged(inp, comp) {
		if (this.getMethods('onChanged')) {
			this.executeMethod('onChanged', inp, comp, this.eventType());
		}
	}

    checkCurrentRadio() {
		this.eveType = 'key';
		this.setData('ltPropChecked', true);
		this.eveType = '';
	}

    checkNextRadio() {
		var radios = this.extractAllRadios(),
			nextRadioIndex = this.getCheckableRadio(radios, 1);

		if (nextRadioIndex !== -1) {
			radios[nextRadioIndex].component.eveType = 'key';
			radios[nextRadioIndex].ltProp('checked', true);
			radios[nextRadioIndex].component.eveType = '';
		}
	}

    checkPreviousRadio() {
		var radios = this.extractAllRadios(),
			previousRadioIndex = this.getCheckableRadio(radios, -1);

		if (previousRadioIndex !== -1) {
			radios[previousRadioIndex].component.eveType = 'key';
			radios[previousRadioIndex].ltProp('checked', true);
			radios[previousRadioIndex].component.eveType = '';
		}
	}

    getCheckableRadio(radios, indexChange) {
		var currentRadioIndex = this.getIndex(radios),
			totalRadios = radios.length,
			index = currentRadioIndex;

		for (var i = 0; i < totalRadios; i++) {
			index = index + indexChange;

			if (index < 0) {
				index = totalRadios - 1;
			}
			else if (index === totalRadios) {
				index = 0;
			}

			if (!radios[index].ltProp('disabled') || index === currentRadioIndex) {
				return index;
			}
		}

		return -1;
	}

    extractAllRadios() {
		var name = this.getData('ltPropName'), i = 0, radio,
			inp = window._lyteUiUtils.querySelectorAll('input[type="radio"][name="' + name + '"]'),
			result = [];

		for (; i < inp.length; i++) {
			radio = inp[i];

			while (
				radio
				&& radio.tagName !== 'LYTE-RADIOBUTTON'
				&& radio.tagName !== 'HTML'
			) {
				radio = radio.parentNode;
			}

			if( !radio ) {
				return ;
			}

			if (radio.tagName === 'LYTE-RADIOBUTTON') {
				result.push(radio);
			}
		}

		return result;
	}

    getIndex(result) {
		return result.indexOf(this.$node);
	}

    extractRadios() {
		var name = this.getData('ltPropName'), i = 0, tag,
			inp = this.getFromCache('peers') || [];

		for (; i < inp.length; i++) {
			tag = inp[i];

			if (tag.ltProp('checked')
				&& tag != this.$node
			) {
				return tag;
			}
		}
	}

    changeChecks(change) {
		var name = this.getData('ltPropName'),
			radio = this.extractRadios(),
			inp = radio && radio.querySelector('input'),
			comp = radio && radio.component,
			cur = this.$node.querySelector('input'),
			ret_uncheck, ret_check;

		// A checked radiobutton is checked
		// This check probably never executes because
		// observers never fire when oldValue and newValue are the same. Its just a safety check
		if (change.newValue && change.oldValue) {
			return;
		}

		// A radiobutton that is different from the currently checked radiobutton is checked
		else if (change.newValue) {
			this.setData('prevent', true);
			this.setData('ltPropChecked', false);

			// Unchecking the previously checked radiobutton and calling before uncheck callbacks
			if (radio) {
				ret_uncheck = this.beforeUncheck(cur, this);
			}

			if (ret_uncheck === false) {
				this.revertState(radio, this.$node);

				return;
			}

			ret_check = this.beforeCheck(cur, this);

			if (ret_check === false) {
				this.revertState(radio, this.$node);

				return;
			}

			// calling the uncheck callback of the previously checked radiobutton
			if (radio) {
				comp.setData('prevent', true);
				radio.ltProp('checked', false);
				comp.setData('prevent', false);
				this.uncheck(cur, this);
			}

			this.setData('ltPropChecked', true);
			this.setData('prevent', false);

			this.check(cur, this);
			this.onchanged(cur, this);
		}

		// A radiobutton is unchecked but it was not previously unchecked
		else if (!change.newValue && change.oldValue) {
			this.setData('prevent', true);
			this.setData('ltPropChecked', true);

			ret_uncheck = this.beforeUncheck(cur, this);

			if (ret_uncheck === false) {
				this.revertState(this.$node, {});

				return;
			}

			this.setData('ltPropChecked', false);
			this.uncheck(cur, this);
			this.onchanged(cur, this);
			this.setData('prevent', false);
		}

	}

    change(change) {

		var name = this.getData('ltPropName'),
			input = this.getCheckedInput();

		if (!this.getData('onChange')) {
			this.changeChecks(change);
		}
		else {
			if (!change.newValue) {
				this.uncheck(input, this.getData('second'));
			}
			else {
				this.check(input, this);
			}
		}
	}

    getCheckedInput() {
		var name = this.getData('ltPropName'), peers = this.getFromCache('peers') || [],
			input;

		for (var i = 0; i < peers.length; i++) {
			input = peers[i].querySelector('input');

			if (input.matches(':checked')) {
				return input;
			}
		}
	}

    resetValues() {
		this.setInCache('prev', undefined);
		this.setInCache('node', undefined);
	}

    fireCallbacks(change) {
		var prev = change.oldValue,
			cur = change.newValue,
			preventFocus = this.getData('ltPropPreventFocus');

		if (prev && !cur) {
			this.resetValues();
		}

		if (this.getData('prevent')) {
			return;
		}

		if (cur && !preventFocus) {
			this.$node.focus();
		}

		this.change(change);
	}

    // Returns undefined when node is undefined

    getParentRadio(node) {
		while (node
			&& node.tagName !== 'LYTE-RADIOBUTTON') {
			node = node.parentNode;
		}

		return node;
	}

    revertState(uncheckedRadio, checkedRadio) {
		var uncheckedRadioComp = (uncheckedRadio || {}).component,
			checkedRadioComp = (checkedRadio || {}).component;

		if (uncheckedRadioComp) {
			uncheckedRadioComp.setData('prevent', true);
			uncheckedRadioComp.setData('ltPropChecked', true);
			uncheckedRadio.querySelector('input').checked = true;
			uncheckedRadioComp.setData('prevent', false);
		}

		if (checkedRadioComp) {
			checkedRadioComp.setData('prevent', true);
			checkedRadioComp.setData('ltPropChecked', false);
			checkedRadio.querySelector('input').checked = false;
			checkedRadioComp.setData('prevent', false);
		}

	}

    callMethodsOnInit() {
		var foi = this.getData('ltPropFireOnInit'),
			checked = this.getData('ltPropChecked');

		if (!foi) {
			return;
		}

		if (checked) {
			var element = this.$node.querySelector('input'),
				value = element.getAttribute('value');

			this.beforeCheck(element, this);
			this.check(element, this);
			this.onchanged(element, this);
		}
	}

    checkFR(node) {

		node.component.setData('ltPropChecked', true);
		node.querySelector('input').checked = true;

		node.component.fireCallbacks({
			oldValue: false,
			newValue: true
		});
	}

    uncheckFR(node) {

		node.component.setData('ltPropChecked', false);
		node.querySelector('input').checked = false;

		node.component.fireCallbacks({
			oldValue: true,
			newValue: false
		});
	}

    eventType() {
		var isClicked = this.clicked || this.clickFn,
			key = this.eveType;

		if (isClicked) {
			return 'click';
		}

		return key ? key : 'script';
	}

    revertToPreviousState() {
		var node = this.getFromCache('node'),
			uncheckedRadio = this.getParentRadio(node),
			checkedRadio = this.$node;

		this.revertState(uncheckedRadio, checkedRadio);
		this.setInCache('node', undefined);

		// Just a safety
		this.clicked = false;
	}

    static actions(arg1) {
        return Object.assign(super.actions({
            preventPropagation: function () {
                if (event.keyCode === 32) {
                    this.preventClick = true;
                }
            },

            mup: function (event) {
                var name = this.getData('ltPropName'),
                    checkedNode = window._lyteUiUtils.querySelector('input[type="radio"][name="' + name + '"]:checked'), ret_check, ret_uncheck;

                this.setData('shouldCallUnChecked', false);
                if (checkedNode) {
                    this.setInCache('prev', checkedNode.getAttribute('value'));
                    this.setInCache('node', checkedNode);
                    this.setData('shouldCallUnChecked', true);
                }

                var element = this.$node.querySelector('input'), checked = element.checked;

                if (element.disabled) {
                    return;
                }

                if (!checked) {
                    this.clicked = true;

                    if (this.getData('shouldCallUnChecked')) {
                        ret_uncheck = this.beforeUncheck(element, this);

                        if (ret_uncheck === false) {
                            this.setData('preventStateChange', true);
                            this.clicked = false;
                            return;
                        }
                    }

                    ret_check = this.beforeCheck(element, this);

                    if (ret_check === false) {
                        this.setData('preventStateChange', true);
                        this.clicked = false;
                        return;
                    }
                }
            },
            checkboxClick:function(_this,event){
            $L(_this).addClass('lyteCheckboxActive');
            },
            valueChanged: function (event) {
                var ele = event.target,
                    val = ele.getAttribute('value'),
                    prev = this.getFromCache('prev'),
                    node = this.getFromCache('node'),
                    preventStateChange = this.getData('preventStateChange'),
                    comp = node && node.parentElement.parentElement.component;

                event.stopPropagation();

                if (this.preventClick) {
                    this.preventClick = false;
                    return;
                }

                if (val === prev) {
                    return;
                }

                if (preventStateChange) {
                    this.setData( 'preventStateChange', false );
                    this.revertToPreviousState();

                    return;
                }

                if (node) {
                    while (node.tagName !== 'LYTE-RADIOBUTTON') {
                        node = node.parentNode;
                    }

                    comp.setData('onChange', true);

                    // We are using a variable called second so that we can pass the current radiobutton's this to the callback.
                    // We won't get the current radiobutton's this in a different radiobutton's observer - we are setting ltPropChecked to false below.
                    comp.setData('second', this);

                    if (node._fR) {
                        this.uncheckFR(node);
                    }
                    else {
                        node.ltProp('checked', false);
                    }

                    comp.setData('second', undefined);

                    comp.setData('onChange', false);
                }

                node = this.getFromCache('node');
                if (node) {
                    this.setInCache('node', undefined);
                }

                this.setData('onChange', true);

                if (this.$node._fR) {
                    this.checkFR(this.$node);
                }
                else {
                    this.setData('ltPropChecked', true);
                }

                this.setData('onChange', false);
                this.onchanged(ele, this);
                this.clicked = false;
            }
        }), arg1);
    }

    static observers(arg1) {
        return Object.assign(super.observers({
            ariaObserver: function (change) {
                var oldAria = change.oldValue,
                    newAria = change.newValue;

                this.addAriaValues(oldAria, newAria);
            }.observes('ltPropAriaRadio'),

            changeStyle: function () {
                this.changeStyleFunction()
            }.observes('ltPropType'),

            disabledChange: function () {
                this.reduceOpacity();
            }.observes('ltPropDisabled'),

            nameObserver: function () {
                this.initCache();
            }.observes('ltPropName'),

            valueChanged: function (change) {
                this.fireCallbacks(change);
            }.observes('ltPropChecked'),

            focusRadioButton: function () {
                var shouldFocus = this.getData('ltPropFocus');

                if (shouldFocus) {
                    this.$node.focus();
                }

                this.data.ltPropFocus = false;
            }.observes('ltPropFocus').on('didConnect')
        }), arg1);
    }

    _() {
        _;
    }
}

LyteRadiobuttonComponent._template = "<template tag-name=\"lyte-radiobutton\"> <template _new=\"true\" value=\"{{ltPropType}}\" is=\"switch\">      <template case=\"default\" is=\"case\"></template><template case=\"primary\" is=\"case\"></template><template case=\"secondary\" is=\"case\"> <label class=\"{{radioclass}}\" onmouseup=\"{{action('mup',event)}}\"> <input tabindex=\"{{ltPropTabindex}}\" id=\"{{ltPropId}}\" aria-labelledby=\"{{randomId}}\" onkeypress=\"{{action('preventPropagation',event)}}\" type=\"radio\" name=\"{{ltPropName}}\" value=\"{{ltPropValue}}\" data-tabindex=\"{{ltPropDataTabindex}}\" class=\"lyteHide\" onclick=\"{{action('valueChanged',event)}}\" disabled=\"{{expHandlers(ltPropDisabled,'||',ltPropReadOnly)}}\" checked=\"{{ltPropChecked}}\"> <span class=\"{{ltPropClass}}\"> <span class=\"lyteRadioCheck\"> </span> </span> <span class=\"{{ltPropLabelClass}}\" id=\"{{randomId}}\" aria-hidden=\"true\"> <template is=\"switch\" l-c=\"true\" _new=\"true\"><template case=\"{{ltPropYield}}\" is=\"case\" lc-id=\"lc_id_0\"> <lyte-yield yield-name=\"yield\"></lyte-yield> </template><template default=\"\"> {{ltPropLabel}} </template></template></span> </label> </template><template case=\"switch\" is=\"case\"> <label class=\"\" onmouseup=\"{{action('mup',event)}}\"> <input tabindex=\"{{ltPropTabindex}}\" id=\"{{ltPropId}}\" aria-labelledby=\"{{randomId}}\" onkeypress=\"{{action('preventPropagation',event)}}\" type=\"radio\" name=\"{{ltPropName}}\" value=\"{{ltPropValue}}\" data-tabindex=\"{{ltPropDataTabindex}}\" checked=\"{{ltPropChecked}}\" disabled=\"{{expHandlers(ltPropDisabled,'||',ltPropReadonly)}}\" class=\"lyteHide on-off-sw\" onclick=\"{{action('valueChanged',event)}}\"> <span class=\"{{ltPropClass}}\"> <span class=\"on-btn\"></span> </span> <span class=\"{{ltPropLabelClass}}\" id=\"{{randomId}}\" aria-hidden=\"true\"> <template is=\"switch\" l-c=\"true\" _new=\"true\"><template case=\"{{ltPropYield}}\" is=\"case\" lc-id=\"lc_id_0\"> <lyte-yield yield-name=\"yield\"></lyte-yield> </template><template default=\"\"> {{ltPropLabel}} </template></template></span> </label> </template><template case=\"slider\" is=\"case\"> <label class=\"lyteRadioSliderLabel\" onmouseup=\"{{action('mup',event)}}\"> <input tabindex=\"{{ltPropTabindex}}\" id=\"{{ltPropId}}\" aria-labelledby=\"{{randomId}}\" onkeypress=\"{{action('preventPropagation',event)}}\" type=\"radio\" name=\"{{ltPropName}}\" data-tabindex=\"{{ltPropDataTabindex}}\" value=\"{{ltPropValue}}\" checked=\"{{ltPropChecked}}\" disabled=\"{{expHandlers(ltPropDisabled,'||',ltPropReadonly)}}\" class=\"lyteHide\" onclick=\"{{action('valueChanged',event)}}\"> <span class=\"{{ltPropClass}}\"> <span class=\"{{ltPropLabelClass}}\" id=\"{{randomId}}\" aria-hidden=\"true\"> <template is=\"switch\" l-c=\"true\" _new=\"true\"><template case=\"{{ltPropYield}}\" is=\"case\" lc-id=\"lc_id_0\"> <lyte-yield yield-name=\"yield\"></lyte-yield> </template><template default=\"\"> {{ltPropLabel}} </template></template></span> </span> </label> </template><template case=\"checkbox\" is=\"case\"> <label class=\"{{radioclass}}\" onmouseup=\"{{action('mup',event)}}\"> <input tabindex=\"{{ltPropTabindex}}\" id=\"{{ltPropId}}\" aria-labelledby=\"{{randomId}}\" onkeypress=\"{{action('preventPropagation',event)}}\" type=\"radio\" name=\"{{ltPropName}}\" value=\"{{ltPropValue}}\" data-tabindex=\"{{ltPropDataTabindex}}\" class=\"lyteHide\" onclick=\"{{action('valueChanged',event)}}\" disabled=\"{{expHandlers(ltPropDisabled,'||',ltPropReadOnly)}}\" checked=\"{{ltPropChecked}}\"> <span class=\"{{ltPropClass}}\"> <span class=\"lyteRadioCheck\"> </span> </span> <span class=\"{{ltPropLabelClass}}\" onclick=\"{{action('checkboxClick',this,event)}}\" id=\"{{randomId}}\" aria-hidden=\"true\"> <template is=\"switch\" l-c=\"true\" _new=\"true\"><template case=\"{{ltPropYield}}\" is=\"case\" lc-id=\"lc_id_0\"> <lyte-yield yield-name=\"yield\"></lyte-yield> </template><template default=\"\"> {{ltPropLabel}} </template></template></span> </label> </template></template> </template>";;
LyteRadiobuttonComponent._dynamicNodes = [{"t":"a","p":[1]},{"t":"s","p":[1],"c":{"default":{"dN":[],"additional":{"next":"primary"}},"primary":{"dN":[],"additional":{"next":"secondary"}},"secondary":{"dN":[{"t":"a","p":[1],"cn":"secondary"},{"t":"a","p":[1,1],"cn":"secondary"},{"t":"a","p":[1,3],"cn":"secondary"},{"t":"a","p":[1,5],"cn":"secondary"},{"t":"s","p":[1,5,1],"c":{"lc_id_0":{"dN":[{"t":"i","p":[1],"in":0,"cn":"lc_id_0"}],"cdp":{"t":"a","p":[0]},"dcn":true}},"d":{"dN":[{"t":"tX","p":[1],"cn":"default"}]},"dc":{"lc_id_0":{"dc":[0],"hc":true,"trans":true},"default":{}},"hd":true,"co":["lc_id_0"],"hc":true,"trans":true,"in":0,"cn":"secondary"}]},"switch":{"dN":[{"t":"a","p":[1],"cn":"switch"},{"t":"a","p":[1,1],"cn":"switch"},{"t":"a","p":[1,3],"cn":"switch"},{"t":"a","p":[1,5],"cn":"switch"},{"t":"s","p":[1,5,1],"c":{"lc_id_0":{"dN":[{"t":"i","p":[1],"in":0,"cn":"lc_id_0"}],"cdp":{"t":"a","p":[0]},"dcn":true}},"d":{"dN":[{"t":"tX","p":[1],"cn":"default"}]},"dc":{"lc_id_0":{"dc":[0],"hc":true,"trans":true},"default":{}},"hd":true,"co":["lc_id_0"],"hc":true,"trans":true,"in":0,"cn":"switch"}]},"slider":{"dN":[{"t":"a","p":[1],"cn":"slider"},{"t":"a","p":[1,1],"cn":"slider"},{"t":"a","p":[1,3],"cn":"slider"},{"t":"a","p":[1,3,1],"cn":"slider"},{"t":"s","p":[1,3,1,1],"c":{"lc_id_0":{"dN":[{"t":"i","p":[1],"in":0,"cn":"lc_id_0"}],"cdp":{"t":"a","p":[0]},"dcn":true}},"d":{"dN":[{"t":"tX","p":[1],"cn":"default"}]},"dc":{"lc_id_0":{"dc":[0],"hc":true,"trans":true},"default":{}},"hd":true,"co":["lc_id_0"],"hc":true,"trans":true,"in":0,"cn":"slider"}]},"checkbox":{"dN":[{"t":"a","p":[1],"cn":"checkbox"},{"t":"a","p":[1,1],"cn":"checkbox"},{"t":"a","p":[1,3],"cn":"checkbox"},{"t":"a","p":[1,5],"cn":"checkbox"},{"t":"s","p":[1,5,1],"c":{"lc_id_0":{"dN":[{"t":"i","p":[1],"in":0,"cn":"lc_id_0"}],"cdp":{"t":"a","p":[0]},"dcn":true}},"d":{"dN":[{"t":"tX","p":[1],"cn":"default"}]},"dc":{"lc_id_0":{"dc":[0],"hc":true,"trans":true},"default":{}},"hd":true,"co":["lc_id_0"],"hc":true,"trans":true,"in":0,"cn":"checkbox"}]}},"d":{},"dc":{"default":{},"primary":{},"secondary":{"dc":[0],"hc":true,"trans":true},"switch":{"dc":[0],"hc":true,"trans":true},"slider":{"dc":[0],"hc":true,"trans":true},"checkbox":{"dc":[0],"hc":true,"trans":true}},"co":["default","primary","secondary","switch","slider","checkbox"],"hc":true,"trans":true,"in":0},{"type":"dc","trans":true,"hc":true,"p":[0]}];;

LyteRadiobuttonComponent._observedAttributes = [
    "ltPropType",
    "ltPropName",
    "ltPropDisabled",
    "ltPropChecked",
    "ltPropLabel",
    "ltPropValue",
    "ltPropFireOnInit",
    "ltPropLabelClass",
    "ltPropClass",
    "ltPropYield",
    "ltPropAriaRadio",
    "ltPropFocus",
    "ltPropPreventFocus",
    "lyteUnbound",
    "ltPropReadOnly",
    "ltPropDataTabindex",
    "ltPropId",
    "ltPropKeyboardNavigation",
    "ltPropTabindex"
];

if(!window._lyteUiUtils.lyteRadioBtnEvtAdded) {
	window._lyteUiUtils.addGlobalEventListener('keydown', function (event) {
		if( !document.contains( event.target ) ) {
			return ;
		}
		
		var keyCode = event.keyCode, node, checked, comp;

		if (keyCode === 37
			|| keyCode === 38
			|| keyCode === 39
			|| keyCode === 40
			|| keyCode === 32
		) {

			node = document.activeElement;

			if(node.tagName === 'LYTE-RADIOBUTTON-GROUP') {
				node = node.component.getCurrentBtn();
				if(!node.ltProp("checked")) {
					comp = node.component;
					event.preventDefault();
					if(keyCode === 37 || keyCode === 38) {
						comp.checkPreviousRadio();
					}
					else {
						comp.checkCurrentRadio();
					}
					return;
				}
			}

			else if (node.tagName !== 'INPUT') {
				return;
			}

			while (
				node
				&& node.tagName !== 'LYTE-RADIOBUTTON' 
				&& node.tagName !== 'HTML'
			) {
				node = node.parentNode;
			}

			if( !node ) {
				return ;
			}

			if (node.tagName === 'LYTE-RADIOBUTTON') {
				comp = node.component;
				event.preventDefault();
				if(!comp.getData("ltPropKeyboardNavigation")) {
					return;
				}
				switch (keyCode) {
					case 37:
					case 38:
						comp.checkPreviousRadio();
						break;
					case 39:
					case 40:
						comp.checkNextRadio();
						break;
					case 32:
						comp.checkCurrentRadio();
						break;
				}
			}
		}
	});
	window._lyteUiUtils.lyteRadioBtnEvtAdded = true;
}

/**
 * @syntax nonYielded
 * <lyte-radiobutton lt-prop-value="1" lt-prop-label="check me" lt-prop-name="group-1"></lyte-radiobutton>
 */

/**
 * @syntax yielded
 * <lyte-radiobutton lt-prop-value="1" lt-prop-name="group-1" lt-prop-yield="true">
 *     <template is="registerYield" yield-name="yield">
 *         check me
 *     </template>
 * </lyte-radiobutton>
 */



LyteRadiobuttonComponent.register("lyte-radiobutton", {
    hash: "LyteRadiobuttonComponent_6",
    refHash: "C_lyte-ui-component_@zoho/lyte-ui-component_3"
});



/***/ })

}]);
//# sourceMappingURL=vendors-node_modules_zoho_lyte-ui-component_components_javascript_lyte-radiobutton_js.js.map